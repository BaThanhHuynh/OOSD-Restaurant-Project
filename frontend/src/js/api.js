const API_BASE_URL = "http://localhost:8080/api";

// Khai báo toàn cục để các file khác dùng chung
const API_MENU = `${API_BASE_URL}/menu`;
const API_ORDERS = `${API_BASE_URL}/orders`;
const API_TABLES = `${API_BASE_URL}/tables`;
const API_PAYMENT = `${API_BASE_URL}/payment`;