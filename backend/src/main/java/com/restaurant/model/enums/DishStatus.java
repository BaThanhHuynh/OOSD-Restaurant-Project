package com.restaurant.model.enums;

/**
 * Enum đại diện cho trạng thái của món ăn
 * ORDERED -> COOKING -> READY -> SERVED
 */

public enum DishStatus {
    ORDERED,
    COOKING,
    READY,
    SERVED
}